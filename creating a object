e1. Objects in JavaScript
Objects in JavaScript are collections of key-value pairs. The keys are strings (or Symbols), and the values can be any data type, including functions.

Creating Objects
Using Object Literals (Most Common)


const person = {
  name: "John",
  age: 30,
  greet: function() {
    console.log(`Hello, my name is ${this.name}`);
  }
};

console.log(person.name); // John
person.greet(); // Hello, my name is John

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


e1. Objects in JavaScript
Objects in JavaScript are collections of key-value pairs. The keys are strings (or Symbols), and the values can be any data type, including functions.

Creating Objects
Using Object Literals (Most Common)


const person = {
  name: "John",
  age: 30,
  greet: function() {
    console.log(`Hello, my name is ${this.name}`);
  }
};

console.log(person.name); // John
person.greet(); // Hello, my name is John

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


